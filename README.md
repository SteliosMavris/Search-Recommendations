# Search-Recommendations
Using the Trie data structure and Robinhood hashing to implement search suggestions function
Dictionary file shows all recognisable words
Importance file is the text the program reads and saves in a MinHeap the recognisable words that appear the most
The words reccomended have to follow some rules which can be seen in the code (each rule has a function dedicated to it)
